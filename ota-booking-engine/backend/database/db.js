// Placeholder DB module for future PostgreSQL integration.
// For MVP we still use in-memory arrays inside models.

module.exports = {
  connect: async () => {
    console.log("Database connection is not configured yet.");
  }
};
