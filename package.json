{
  "name": "seatnye-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Root config for static Vercel deployment of OTA frontend",
  "scripts": {
    "build": "echo 'No build step required for static OTA frontend'",
    "start": "echo 'Use a static file server for ota-booking-engine/frontend'"
  }
}
